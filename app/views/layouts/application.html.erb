<!DOCTYPE html>
<html>
<head>
  <title>BookSummary</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

  <%# your font awesome kit
  <script src="https://kit.fontawesome.com/99641265cd.js" crossorigin="anonymous"></script>
  %>

  <meta name="turbo-refresh-method" dont-use-content="morph">
</head>

<body class="bg-gray-600">

<main class="mx-auto px-5 max-w-screen-lg">
  <%= render(Nav::BarComponent.new(
    links: [
      #{ text: 'Home', path: root_path},
      { text: 'Summarize', path: request_summary_path },
      { text: 'Settings', path: settings_path }
    ],
    show_devise: true,
    inactive_style: "hover:bg-gray-100 hover:text-blue-600 rounded-md px-3 py-2 text-md font-medium text-white",
    active_style: "bg-yellow-200  text-gray-900 rounded-md px-3 py-2 text-md font-medium")) %>

  <div class="mt-28 text-gray-200">

    <div class="fixed inset-0 px-4 py-10 mt-8 pointer-events-none sm:p-6 sm:items-start sm:justify-end">
      <div class="flex items-center justify-center p-5">
        <% flash.each do |type, data| %>
          <%= render NotificationComponent.new(type: type, data: {title: data, countdown: true}) %>
        <% end %>
      </div>
    </div>



    <%= yield %>
  </div>
</main>
</body>
</html>
